# 草稿区

把尚未准备发布的 Markdown 文章放在这个文件夹中。

这里的文件不会被 Astro 收录为文章，因此不会显示在网站首页、文章列表、标签页、搜索结果或 RSS 中。

准备发布时，将 Markdown 文件从 `src/content/drafts/` 移动到 `src/content/posts/`，然后提交并推送到 GitHub；Vercel 部署完成后，文章就会在网站上显示。

你可以直接复制 `templates/文章模板.md` 来开始写草稿。
